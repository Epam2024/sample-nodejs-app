version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
      #- aws ecr-public get-login-password --region us-east-1 | docker login --username AWS --password-stdin public.ecr.aws/a8z6i9z9
      #- $(aws ecr get-login --no-include-email --region us-east-2)
      #- $(aws ecr get-login --no-include-email --region us-east-2)
      - aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 465406024893.dkr.ecr.us-east-2.amazonaws.com
  build:
    commands:     
      - docker build -t sample-node-app:latest .
      - docker tag sample-node-app:latest public.ecr.aws/a8z6i9z9/sample-node-app:latest
      #- docker build -t sample-node-app:latest .
      #- docker tag sample-node-app:latest 465406024893.dkr.ecr.us-east-2.amazonaws.com/sample-node-app:latest 
  post_build:
    commands:
      - echo Build completed on `date`
      - echo Pushing the Docker image...
      - docker push public.ecr.aws/a8z6i9z9/sample-node-app:latest
      #- docker push 465406024893.dkr.ecr.us-east-2.amazonaws.com/sample-node-app:latest 
      - printf '[{"name":helloworld,"imageUri":"%s"}]' public.ecr.aws/a8z6i9z9/sample-node-app:latest > imagedefinitions.json
artifacts:
  files: imagedefinitions.json

